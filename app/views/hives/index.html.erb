<h1>Listing hives</h1>

<table>
  <tr>
    <th>Name</th>
    <th>email</th>
    <th>Created at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hives.each do |hive| %>
  <tr>
    <td><%= hive.name %></td>
    <td><%= hive.email %></td>
    <td><%= hive.created_at %></td>
    <td><%= link_to 'Show', hive %></td>
    <td><%= link_to 'Edit', edit_hive_path(hive) %></td>
    <td><%= link_to 'Destroy', hive, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hive', new_hive_path %>
